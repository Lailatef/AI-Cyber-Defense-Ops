# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

Not yet started. This directory (`Module-6/siem-queries`) is empty — no files, not yet tracked in git. This is a placeholder CLAUDE.md; update it once real content (queries, scripts, docs) lands here.

## Purpose

Part of the `AI-Cyber-Defense-Ops` repo, which is organized as a series of modules (`Module-3` through `Module-6`), each a self-contained project in its own subdirectory. Based on the directory name, Module 6 is expected to hold SIEM queries (e.g. Sigma, KQL, SPL, or similar), but no content exists yet to confirm the format or scope.

## Sibling modules for reference

Modules 3-5 (`Module-3/mcp-hayabusa`, `Module-4/mcp-detection-kb`, `Module-5/mcp-detection-kb`) are each MCP servers wrapping Hayabusa EVTX scanning plus a growing detection-engineering knowledge base (curated Sigma rules, ATT&CK mappings, playbooks, investigations, YARA rules). Each has its own `CLAUDE.md`, `README.md`, and `.mcp.json`. There is no root-level `CLAUDE.md` or shared code across modules — check the relevant sibling module's `CLAUDE.md` directly if working across modules.
